SERIES GUIDELINE
This is an umbrella folder that entails all projects and pipelines used in the learning python series. 
One environment is created to house all the folders, packages and modules ~ .praco_venv
The list of all the packages used in the venv is in requirements.txt
However, each project or pipeline has its own project pipeline set up.

